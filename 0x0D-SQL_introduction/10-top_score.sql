-- List all records of a table ordered by the values of a specific column (score)
-- Results should show only the score and name column (in this order)
SELECT score, name
FROM second_table
ORDER BY score DESC;
